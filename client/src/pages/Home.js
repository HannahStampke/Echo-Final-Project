import React from 'react';
import {
  ContentContainer,
} from "../styles/HomeStyle";

const Home = () => {

  return (
    <main>
      <ContentContainer>
        homepage
      </ContentContainer>
    </main>
  );
};

export default Home;
