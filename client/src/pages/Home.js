import React from 'react';

import {
  ContentContainer,
} from "../styles/HomeStyle";
import NavBar from '../components/NavBar';

const Home = () => {

  return (
    <main>
      <NavBar>
      </NavBar>
      <ContentContainer>
      </ContentContainer>
    </main>
  );
};

export default Home;
