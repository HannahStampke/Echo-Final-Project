import React, {useState} from "react";

import {
    /* TODO: add all styles from QuizStyle.js */
} from "../styles/QuizStyle";

export default function Quiz({quiz}) {
    return (
        /* TODO: add quiz content here */
    );
}